<%= form_for(@task, method: :patch) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.label "Completed? " %>
  <%= f.check_box :completed %>
  <%= f.submit %>

<% end %>
